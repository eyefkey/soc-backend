# Dependencies and build output are produced inside the image.
node_modules
dist
generated
coverage

# Version control
.git
.gitignore

# Local env — values come from docker-compose / the runtime environment
.env
.env.*
!.env.example

# Agent skill bundles (not needed at runtime)
.agents
.claude
.windsurf
skills-lock.json

# Editor / OS
.idea
.vscode
.DS_Store
*.log
